# SO
.DS_Store
**/.DS_Store

# Credenciales de despliegue (NUNCA subir)
.soylegal360-deploy.env
*.env
.env*

# Tokens de MCP (NUNCA subir — contiene API_TOKEN de Hostinger u otros)
.mcp.json

# Temporales / preview
/tmp/

# Editor
.vscode/
.idea/

# Claude (config local del agente, no del proyecto web)
.claude/scheduled_tasks.lock
